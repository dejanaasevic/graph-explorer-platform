[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[properties]
name = "graph-core"
version = "0.0.1"
description = "Graph visualisation core"
dependencies = ["graph-api>=0.0.1"]